import type { Place } from "../types/place";

export function filterPlacesByZoom(
  places: Place[],
  zoom: number,
  minZoom = 10,
  maxZoom = 18,
) {
  void zoom;
  void minZoom;
  void maxZoom;

  return places;
}
